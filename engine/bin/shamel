#!/usr/bin/env bash
# shamel — SHAMEL AI unified CLI dispatcher
set -euo pipefail
SHAMEL_HOME="${SHAMEL_HOME:-$(cd "$(dirname "$0")/../.." && pwd)}"
export PYTHONPATH="${SHAMEL_HOME}/engine:${PYTHONPATH:-}"
exec python3 -m shamel_tools "$@"
