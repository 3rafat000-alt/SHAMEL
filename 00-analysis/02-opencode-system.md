# تقرير تدقيق — طبقة `.opencode` الأرشيفية (جيل OpenCode)

**المشروع:** شامل / SHAMEL — تدقيق أجيال SOFI AI · تقرير 02
**النطاق:** `/home/es3dlll/Desktop/Lorka/.opencode` + `/home/es3dlll/Desktop/Lorka/opencode.json`
**الوضع:** READ-ONLY · كل الادعاءات مسندة بـ file:line أو أمر+ناتج
**التاريخ:** 2026-07-10

---

## الجرد

**الحجم الكلي:** `du -sh .opencode` → **69M** · عدد الملفات: `find -type f | wc -l` → **3,980**

| المجلد | الحجم | المحتوى | نظام التسمية |
|---|---|---|---|
| `node_modules/` | **63M** (91%) | `@opencode-ai/plugin` 1.17.15 + تبعياته (`package.json`) | — |
| `.sofi-run/` | 3.6M | runtime debris: Caddyfile + سجلات php (`php/PRJ-SAKK.log` وحده 3.5M) + ملفات PID ميتة | — |
| `tools/` | 996K | **114 سكربت bash** تنفيذي لكل وكيل، في 15 مجلد غرفة | **الجديد** (arc·bck·brd·dat·dsn·fnt·gtw·knw·mob·obs·ops·qa·res·sec·str) |
| `agents/` | 520K | **68 ملف persona** (67 وكيل غرف + `translator/translator.md`) في 10 غرف | **القديم المرقّم** (str-01·uxr-02·dsn-03·mob-04·bkd-05·gtw-06·dat-07·ops-08·sec-09·knb-10) |
| `commands/` | 220K | **54 أمر** (`arc-fix` … `str-rm` + `new`/`fix`/`rm`/`deploy`…) | **الجديد** |
| `engine/` | 196K | 35 ملف: constitution، org-chart، registry.yaml، gates.yaml + 9 gate checklists، hooks (pre/post-tool.sh = 84 سطر)، operations docs | **مختلط** (انظر «الصحة») |
| `sofi-engine/` | 184K | 23 ملف bash orchestration (orchestrator، task-queue، workflow-engine، git-guard، translator…) | القديم — **معلن legacy ذاتياً** |
| `skills/` | 168K | 20 ملف: 10 SKILL.md غرف قديمة + `qa/` (8 مهارات جديدة منها `browser-eyes`) + `sofi-v6-org` + `sofi-v6-gate-flow` | **مختلط** |
| `memory/` | 4K | 4 ملفات jsonl/json **كلها فارغة** (`wc -l` → 0 total) | — |

**agents القديم (68):** التوزيع 7+7+7+6+7+6+7+7+7+6 = 67 + translator. الشخصيات غنية بالعربية — مثال `agents/bkd-05/bkd-05-api-developer.md:10-15` («كريم فاروق المحمد — مطور APIs — الغرفة BKD-05»)، بترويسة OpenCode (`mode: subagent`, `model: opencode/big-pickle`, `permission.edit: allow`).

**ملاحظة git حاسمة:** الطبقة كلها **خارج git** — `git check-ignore -v .opencode` → `.gitignore:8: .opencode/` و`.gitignore:9: opencode.json`، و`git ls-files .opencode | wc -l` → **0**، و`git log --all -- .opencode` فارغ. أي حذف = ضياع نهائي بلا تاريخ.

### opencode.json — ماذا يضبط
- `username: SOFI-AI` · `default_agent: brd-ceo` (opencode.json:3-4).
- `instructions`: 10 ملفات تُحقن في كل جلسة (CLAUDE.md + engine/identity + engine/operations + governance) — opencode.json:5-16.
- `skills.paths`: `.opencode/skills` + `.config/opencode/skills` (opencode.json:17-22).
- خريطة `agent:` تعلن **105 وكيلاً بالمخطط الجديد** (brd-ceo → knw-brain-query) لكن بحقلَي `hidden` + `description` فقط — بلا prompt أو model أو persona (opencode.json:23-129).

---

## الصحة

### 1. الانفصام الموثّق — agents قديم vs commands/tools جديد: **مؤكَّد، وأسوأ من الموصوف**
فحص عيّني لـ10 أوامر: استخراج كل مرجع وكيل (`agent:` frontmatter + `@refs`) والبحث عن ملفه في `.opencode/agents/`:

| الأمر | المراجع | dangling |
|---|---|---|
| bck-new | 7 | 7 |
| fnt-fix | 8 | 8 |
| arc-new | 7 | 7 |
| qa-new | 2 | 2 |
| sec-scan | 4 | 4 |
| obs-incident | 5 | 5 |
| dsn-new | 8 | 8 |
| res-new | 7 | 7 |
| str-new | 7 | 7 |
| knw-doc | 6 | 6 |

**المجموع: 61/61 = 100% dangling.** ولا أمر واحد من الـ54 يشير إلى المخطط القديم: `grep -rl 'bkd-05\|uxr-02\|knb-10' commands/ tools/` → **0 ملف**. الطبقتان لا تتقاطعان إطلاقاً — أوامر تنادي 105 وكيلاً لا ملفات لهم على القرص، و68 persona يتيمة لا يناديها شيء. خريطة `agent:` في opencode.json ترقّع الأسماء شكلياً لكنها تُسقِط التفويض على قشور بلا هوية.

### 2. ثلاثة سجلات تنظيمية متناقضة داخل الطبقة نفسها
- `engine/README.md:4` يعلن «105 agents · 15 rooms».
- `engine/agents/registry.yaml:1` يعلن «70 agents, 10 rooms» — والعدّ الفعلي لقوائم `agents:[…]` فيه **67** (تناقض داخلي ثالث).
- `engine/identity/org-chart.md:4` يعلن «**70 agents** · **10 rooms**».

### 3. مسارات ميتة في `opencode.json`
من 10 ملفات `instructions` المعلنة، اثنان مفقودان (تحقق `[ -e … ]` → MISS): `AGENTS.md` (الجذر) و`.opencode/engine/agents/BRIEFING.md`.

### 4. محرّكان معلنان legacy ذاتياً
- `sofi-engine/README.md:1-3`: «⛔ This directory is **legacy**. Superseded by `orchestrator/` (Python)» + جدول هجرة كامل.
- `tools/README.md:1-3`: «⛔ These bash/Python scripts are **legacy**. Superseded by `orchestrator/tools/`» — والمفارقة أن «الجديد» المُشار إليه يستعمل الترميز القديم (`orchestrator/tools/bkd_05/`).

### 5. runtime debris حي الشكل ميت الفعل
`.sofi-run/caddy/Caddyfile:1` يقول «generated by `sofi domain`» ويستورد `sites/*.caddy` من داخل `.opencode` — لكن أداة domain الحية في v6 (`company/os/sofi_tools/domain.py:50` بالـWT) تشير إلى `.claude/.sofi-run` (غير موجود بعد)، وcaddy الجاري فعلاً يعمل بـ`/etc/caddy/Caddyfile` (ناتج `pgrep -a caddy`)، وPID المسجّل في `php/PRJ-SAKK.pid` (123186) **ميت** (`ps -p` فارغ).

### 6. ذاكرة فارغة
`memory/{audit,events,sessions}.jsonl` + `edit-tracker.json` كلها 0 سطر — النظام لم يعمل جدياً بهذه القناة أو مُسِح.

---

## نقاط القوة

1. **`tools/` — الكنز الفعلي:** 114 سكربت bash تنفيذي per-agent على **التسمية الجديدة**، عملي لا توثيقي — مثال `tools/bck/api-engineer/endpoint-scaffold.sh` يولّد FormRequest+Controller+Service+Resource+test من اسم endpoint؛ ومثله `dat/db-engineer/migration-consolidate.sh`، `obs/sre/sli-calc.sh`، `fnt/a11y-engineer/a11y-audit.sh`.
2. **`skills/qa/browser-eyes`** — مهارة فحص بصري (متصفح حقيقي + screenshots + DOM) موصولة بسكربت حي موجود فعلاً: `projects/PRJ-SAKK/tests/e2e/browser-eyes.sh` (4.9K، تنفيذي) — **لا مقابل لها في `.claude/skills`** (`find -iname '*browser*'` → 0).
3. **9 gate checklists** في `engine/lifecycle/checklists/gate-{0..8}.md` — تفصيل per-gate قد يكمّل `company/nexus/gates.yaml`.
4. **personas عربية غنية** في agents/ (أسماء، أعمار، خلفيات) — قيمة أسلوبية/تاريخية.
5. عزل نظيف: الطبقة كلها خلف `.gitignore` فلا تلوّث تاريخ git.

---

## نقاط الضعف

1. **غير قابلة للتشغيل:** 100% dangling refs بين commands وagents؛ التفويض يسقط على وكلاء `opencode.json` بلا prompt/persona.
2. **63M node_modules** (91% من الحجم) لطبقة ميتة — و`package.json` نفسه متجاهَل في `.opencode/.gitignore` فلا إعادة بناء من git.
3. **ثلاث حقائق تنظيمية متضاربة** (105/15 vs 70/10 vs 67 فعلي) تضلّل أي قارئ جديد.
4. **هشاشة قصوى:** الأصول الفريدة (tools/ الـ114 + browser-eyes) موجودة **فقط** على هذا القرص خارج أي git.
5. سجلات 3.5M وPIDs ميتة داخل الطبقة توحي زوراً بأنها بنية تشغيل حية.

---

## التداخل مع الطبقات الأخرى

| مكوّن .opencode | المقابل | العلاقة |
|---|---|---|
| `commands/` (54) | `/home/es3dlll/Desktop/Lorka/.claude/commands/` (54) | **مطابقة بايت-ببايت** (`diff -q bck-new.md` → IDENTICAL) — نسخة زائدة |
| `skills/sofi-v6-{org,gate-flow}` | `.claude/skills/` بنفس الاسمين | موجودان لكن **يختلفان محتوىً** (`diff -q` → differ) — نسخة `.claude` هي المرجع الحي |
| `agents/` (68 قديم) | WT `.claude/agents/` (105) + `org-rooms/` (100 persona) | متجاوَز بالكامل |
| `engine/` (constitution·org-chart·gates) | WT `company/constitution/` + `company/nexus/` | متجاوَز — v6 أحدث وأدق |
| `sofi-engine/` | `orchestrator/` بجذر MAIN (موجود، `ls` أكد) | معلن legacy ذاتياً نحوه |
| `tools/` (114 bash) | WT `tools/` (إطار Python، 30 ملف — `diff -rq` كله "Only in"، **صفر تقاطع**) + `company/rooms/*/tools/` (**15 README placeholder فقط**) | **لا مقابل وظيفي في v6** |
| `.sofi-run/` | `domain.py:50` الحي يتوقع `.claude/.sofi-run` | مسار متروك؛ caddy الجاري على `/etc/caddy` |

---

## ما يُرحَّل لنظام شامل

**إنقاذ (لا وجود له في v6):**
1. **`tools/` كاملاً (114 سكربت)** — يسدّ مباشرةً فجوة `company/rooms/*/tools/` الفارغة؛ التسمية أصلاً على مخطط v6 فالترحيل شبه مجاني. **أولوية قصوى + إدخاله git فوراً قبل أي تنظيف.**
2. **`skills/qa/browser-eyes`** + سكربته `projects/PRJ-SAKK/tests/e2e/browser-eyes.sh` — قدرة QA بصرية فريدة (تعميمها لتقبل أي PRJ لا PRJ-SAKK فقط).
3. **`engine/lifecycle/checklists/gate-{0..8}.md`** — مراجعة ودمج ما يزيد على `company/nexus/gates.yaml`.
4. (انتقائي) صياغات personas العربية من `agents/` كمرجع أسلوبي لأشخاص org-rooms.

**مكرر/أدنى — لا يُرحَّل:**
`commands/` (نسخة مطابقة في `.claude/commands`) · `sofi-engine/` (legacy معلن) · `engine/` identity+operations (دستور v6 أشمل) · `agents/` القديمة · `node_modules/` + `.sofi-run/` (حذف آمن) · `memory/` (فارغة) · `opencode.json` (خاص بمشغّل OpenCode المهجور).

**تكلفة البقاء:** 69M قرص (تنخفض لـ~6M بحذف node_modules)، والأغلى إدراكياً: 3 سجلات متناقضة + طبقة runtime زائفة + 54 أمراً مكرراً — كلها تلوّث أي تدقيق أو grep مستقبلي. التوصية: **انتشال البنود 1–3 إلى git ثم أرشفة/حذف الباقي.**

---

## الحكم

**LEGACY** — جيل مهجور مُعلَن التقاعد ذاتياً (README-ات ⛔) وغير قابل للتشغيل (100% dangling refs بين commands وagents + سجلات متناقضة 105/70/67)، لكنه يحوي أصولاً فريدة غير مكررة في v6 (114 أداة bash + browser-eyes) موجودة فقط خارج git ويجب انتشالها قبل أي حذف.
