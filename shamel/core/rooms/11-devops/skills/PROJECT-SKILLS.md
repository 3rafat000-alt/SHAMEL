# DevOps room — PRJ-SAKK project skills

Canonical location: `~/Desktop/SHAMEL/.claude/skills/<name>/SKILL.md`

- **mcp-capability-map** — ranked shortlist of MCP servers surveyed from
  mcpmarket.com that would help this specific stack (Laravel/PHP, Flutter,
  bare-metal php-fpm+Caddy — explicitly NOT Docker/K8s). Includes an SSH MCP
  candidate flagged as the single highest blast-radius item on the list —
  do not add SSH/infra access for an agent without an explicit, narrowly
  scoped, owner-approved decision (which host, which user, which commands).
  Also confirms: no viable SMS-OTP MCP exists for Syria (Twilio stopped
  serving Syrian numbers 2025-09-15) — that's a build item, not an install.
