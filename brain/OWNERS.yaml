# brain/OWNERS.yaml — one-to-one ownership map for every brain file
# Every entry MUST match a file's frontmatter `owner:` field.
# doctor --brain verifies this match fail-closed (GAP-08).
version: 1
owners:
  BRAIN.md:
    owner: knw-lead
    description: "Design law — the brain constitution"
  OWNERS.yaml:
    owner: knw-lead
    description: "This ownership matrix — self-referential entry"
  org/DECISIONS.md:
    owner: knw-historian
    description: "Architecture Decision Record log"
  org/EVOLUTION.md:
    owner: knw-lead
    description: "Org evolution timeline"
  org/HANDOFFS.md:
    owner: gtw-dispatcher
    description: "Cross-room handoff index"
  org/LESSONS.md:
    owner: knw-reflector
    description: "Distilled lessons (sig format: LES-NNN·sig)"
  org/PERSONAS.md:
    owner: knw-memory-curator
    description: "Canonical persona registry (mirrors personas.yaml)"
  org/TEAM_STATUS.md:
    owner: gtw-dispatcher
    description: "@generated — per-room health summary"
  templates/CONTEXT.md:
    owner: knw-doc-writer
    description: "Context template for project brain"
  templates/DECISIONS.md:
    owner: knw-historian
    description: "Decision log template"
  templates/FOUNDATIONS.md:
    owner: knw-lead
    description: "Project foundations template"
  templates/FOLDER-MAP.md:
    owner: knw-doc-writer
    description: "@generated — directory map template"
  templates/HANDOFFS.md:
    owner: gtw-dispatcher
    description: "Handoff ticket template"
  templates/LESSONS.md:
    owner: knw-reflector
    description: "Lessons template (sig format)"
  templates/LOCKS.md:
    owner: gtw-gatekeeper
    description: "Gate-lock tracking template"
  templates/STATE.md:
    owner: gtw-gatekeeper
    description: "Project state template"
  db/brain.db:
    owner: knw-memory-curator
    description: "FTS5 session memory database"
  db/sessions.jsonl:
    owner: knw-memory-curator
    description: "Session breadcrumb log"
